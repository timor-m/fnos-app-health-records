# Changelog

## 0.2.6 - Unreleased

### Added

- 报告详情新增成员归属提醒：OCR 提取到的患者性别、出生日期或年龄（儿童体检报告通常只印年龄）与当前成员资料冲突时给出提示，可一键归属到出生日期或年龄一致的其他成员，成员不存在时可直接创建家庭成员并完成归属；归属时自动清理旧成员档案中来源为该报告的血型并迁移相关通知，提醒可忽略且不再重复出现。
- “创建新成员并归属”表单自动带入识别到的患者性别和出生日期；报告只印年龄时按报告日期倒退月龄预填近似出生日期（岁级年龄误差太大不预填），并提示核对后再保存。
- 报告处理中新增“中断”按钮：排队任务立即停止，正在执行的任务在当前步骤结束后停止且不再发起新的 AI 请求，已保存的报告数据和趋势不受影响。
- AI 配置新增“解析程度”选项：概览（默认，合并解析单元、跳过叙事章节和复核，指标与形态发现照常提取，Token 消耗更低）与详细（完整提取叙事章节并做遗漏复核）；模型输出能力允许时概览可单请求完成整份报告解析。
- AI 配置新增 MiniMax Provider 预设，默认使用中国大陆 OpenAI-compatible 地址和 `MiniMax-M2.7` 文本模型；适配推理内容分离、结构化结果校验及输出参数，并明确阻止将当前 M2 系列误用于视觉增强。
- “从 NAS 导入”文件列表增加按需缩略图和大图预览，支持常见图片及 PDF 第一页，预览缓存仅保存在系统临时目录。
- 接入 fnOS 用户个人文件授权：普通用户可在应用内选择并授权自己的报告文件或目录，目录浏览、预览和导入均按当前网关用户执行 ACL 校验。

### Changed
- 重构项目文档：README 聚焦应用介绍、功能、截图、部署入口和交流群；新增 fnOS 安装、通用配置、备份恢复、OCR/AI 使用和常见问题文档，开发与运维细节继续独立维护。
- fnOS 安装文档明确以飞牛应用中心作为正式安装、下载和更新入口，`.fpk` 手动安装仅作为开发测试或备用方式。
- AI 配置页的快速测试、完整测试和保存按钮在触屏端保持单行三列排列。
- AI 模型配置新增单次请求超时参数，默认 600 秒，可按本地模型性能在 30～3600 秒内调整；新配置替代仅依赖部署环境变量的方式。
- fnOS 继续兼容管理员共享授权目录，Docker 继续使用管理员只读挂载目录；个人文件授权仅在支持开放 API 的 fnOS 环境启用。

### Fixed

- 修复"序号"列占据表格首格时整表检验指标丢失的问题：候选判定和字典匹配此前只看第一格，序号格导致数据行不成为候选，AI 提取的正确结果在证据校验阶段被全部拒绝（微量元素等单字项目名报告表现为解析后无指标）；现在统一顺延到真正的项目名格，单字项目名在单位被 OCR 识别坏时也保留候选资格。
- 修复 DeepSeek v4 默认开启思考模式导致解析请求思维链烧光输出预算、JSON 频繁截断的问题：结构化提取请求显式关闭思考模式，旧模型不认识该参数时自动降级重试，Token 消耗大幅下降。
- 修复低输出上限模型（如 DeepSeek 默认 8k）解析多页报告时“输出截断→拆分解析单元→再次截断”的级联问题：规划阶段按当前模型输出能力打包单元，单元拆分限制为 2 级，超出后给出明确错误提示而非反复重试。
- 修复 AI 提取的报告总结可能残留空章节骨架的问题：模型只照抄章节标题（如"【肝胆功能】"）却丢弃标题下内容时，后处理会移除这些空标题，避免详情页展示残缺结构。
- 修复形态变化中“归为同一项目”后项目名称可能被来源记录覆盖，以及待确认记录“作为新项目”后因部位或类型不完整而从列表消失的问题。
- 修复 Ollama Provider 虽标注免 API Key、实际却被判定为配置不完整的问题；空 Key 不再发送无效鉴权头，并兼容未保存服务地址测试、NAS 上模型冷启动耗时，以及不接受 `json_object` 的本地 OpenAI-compatible 服务。
- 修复长时间本地 AI 推理仍可能被 Node HTTP 客户端默认 300 秒响应头/响应体超时提前中断的问题；底层传输超时现在跟随应用的 AI 请求时限，并能正确识别和提示传输层超时。
- 修复 PC 端 AI 配置中视觉模型输入框受双列网格和刷新按钮宽度挤压而错位换行的问题。
- 修复日期滚轮可横向拖动及年份仅覆盖当前年份前后 10 年的问题；所有滚轮锁定为纵向滚动，出生日期支持选择 1900 年至今年。
- 修复在飞牛 App 等内嵌 WebView 中点击"下载 PDF"（报告原件、备份文件）无反应或整页跳转到无法打开页面的问题：识别 WebView 环境后，合成下载点击无法确认生效时自动降级 `window.open` 交给系统浏览器或下载器接管，下载锚点同时改为新窗口打开以避免整页跳转丢失应用状态；完整浏览器行为不变。

## 0.2.5

### Changed

- 日期和日期时间输入统一使用 Apple 风格滚轮选择器，PC 端改为触发控件下方的锚定弹层，触屏端使用底部面板，并适配小屏、刘海安全区和报告筛选场景。
- 日期滚轮改为中心选项清晰、上下渐隐的透明遮罩效果，移除选中框，月份滚动期间冻结日列，停止滚动后再更新并校正无效日期。
- OCR 原件对照在 PC 端扩展为宽屏弹窗，原件和 OCR 文本列获得更充足的查看空间；触屏端继续使用全宽底部弹窗。
- 全部结构化指标工作区支持点击整条指标同步更新三列：原件定位到证据页并高亮 OCR 区域，同时打开对应指标编辑表单。
- 报告列表统一按实际展示日期排序：报告日期缺失时使用审核、接收、检查、采样或申请时间，并新增报告日期正序和上传时间倒序选项；游标分页与排序键保持一致。

### Fixed

- 修复报告详情原件参考没有高亮的问题：详情接口现在补齐证据页 ID 和 OCR 行 ID，兼容表格证据及历史引用文本回匹配。
- 修复日期选择器月份滚动时日列闪烁的问题。

## 0.2.4

### Added

- 远程指标字典 revision 11 补充有核红细胞比率、CA125、ProGRP、肌钙蛋白 I、血清淀粉样蛋白 A、血氨、甘胆酸、肺炎支原体 IgM、脂蛋白(a)、载脂蛋白 A1/ApoB 和胃泌素 17、超氧化物歧化酶等跨机构常见项目。
- 修复报告项目名称带有 `3.`、`（3）`、`No.3` 等表格序号时无法正常匹配指标字典的问题；重新归一化历史数据后会自动回填已有标准指标。

### Changed

### Fixed

- 修复报告项目名称带有 `3.`、`（3）`、`No.3` 等表格序号时无法正常匹配指标字典的问题；重新归一化历史数据后会自动回填已有标准指标。
- 修复 `(N)` 正常标记、结果值拼接、异常状态和体检结论前缀污染指标名称时无法命中字典的问题。
- 修复触屏端上传报告入口按钮宽度不协调的问题，“从 NAS 导入”现在横跨整行，与上方选择文件、拍照两个按钮的总宽度对齐。

### Added

- 指标远程字典补充常见实验室指标、分类和别名，更新后可在本地回填历史趋势数据。
- 报告原件支持异步生成 PDF：上传 PDF 时保留原文件，图片报告按整份报告合并导出，并提供生成状态查询。
- AI 配置新增 Ollama Provider，支持配置本地 Ollama 服务和模型进行报告整理。

### Changed

- 优化 AI Provider 配置、模型连接测试和 API Key 校验，兼容 Ollama 的无 API Key 场景。

### Fixed

- 修复报告原件导出和 PDF 生成相关接口在长耗时任务下的处理流程。

## 0.2.3

### Added

- 报告详情的结构化指标保留前 24 项紧凑预览，超过时在列表末尾提供“查看全部”入口；完整指标使用独立弹窗展示，支持固定标题栏和 PC、触屏端滚动浏览。
- 重复报告检测前置到自动 AI 入队之前：全部 OCR 完成后先在设备内比较原件指纹、OCR 医疗编号、去噪文本指纹和带结果的数据行；高度重复时暂缓自动 AI、生成提醒和详细日志，中可信候选只提示不阻断，用户仍可在报告详情中手动继续整理。
- 新增独立 AI Runtime、AI 场景注册表和统一任务路由层：Provider 协议调用、场景模型选择与报告领域解析不再耦合，为后续报告摘要、指标解释和重复报告辅助等能力提供稳定扩展入口。
- AI 配置增加按场景绑定 Provider 和模型的能力；未单独设置的场景继承默认模型，所有场景复用对应 Provider 已加密保存的 API Key，切换模型不会丢失其他 Provider 配置。
- 新增 AI 提取前的 OCR 输入规划器：按 OCR 原始行顺序重建完整页面，过滤患者姓名、证件号、电话、地址、邮箱和精确出生日期，同时保留报告号、门诊号、住院号、体检号、检查号、标本号与条码号等业务编号。
- OCR 输入支持稳定解析单元规划：指标内容按最多 12 个完整页面、约 8,000 字符、预估 12,000 输出 Token 和 60 个疑似数据行动态装箱，稀疏叙事内容最多可跨 24 页合并；单页自身超限时仍保持整页内容，不拆断原报告的表格和章节关系。每个单元生成稳定 Key、输入哈希和整份计划哈希，为并发调用、断点恢复和结果合并提供基础。
- 增加报告级 AI 输入计划诊断接口，可查看脱敏页面、解析单元、页码范围、字符数、候选行数和稳定哈希，便于验证长 PDF 在进入 AI 前是否完整切割。
- 增加独立的 AI 入参调试日志：开发环境默认在 `.data/logs/ai-input-debug.log` 记录实际发送的模型、系统提示词和脱敏 OCR 文本，生产环境默认关闭；日志不包含 API Key、认证头或响应正文，使用独立大小上限和轮转文件。
- 指标问题池支持按名称聚合、批量选择并打开预填 GitHub Issue，外发内容严格限制为指标名称；增加远程字典发布命令、手工 Issue 模板和字典 PR 轻量校验工作流，串起 revision、校验、manifest 与 GitHub Pages 发布流程。
- 远程字典“检查更新”会校验完整快照并展示新增、变更、移除指标及别名和分类差异，更新时再次验证，安装或回滚后立即本地回填历史指标。
- 新增项目内标准指标字典目录：核心层与远程层分别维护完整的 `taxonomy.json`、`indicators.json`，并提供 JSON Schema、跨文件引用校验、版本差异比较、SHA-256 清单及可选 Ed25519 签名生成工具；GitHub Actions 可将通过校验的远程完整快照发布到 GitHub Pages。
- “我的 → 系统日志”增加管理员诊断包导出：一次性打包脱敏后的应用运行、OCR 安装、fnOS 启停日志和运行环境摘要，每个日志文件最多保留末尾 2 MB，不包含数据库、报告、OCR 正文、AI 配置密钥或用户身份资料。
- 增加回收站后台维护执行器：应用启动后及每 6 小时分批清理超过 30 天的报告，并每周扫描一次未被数据库引用的历史孤儿文件。
- 增加文件 GC 队列和数据库 v10 迁移：单页、整份报告删除后延迟清理原件与缩略图，删除前按实时引用确认共享 PDF 已无其他页面使用，失败文件会自动重试。
- 增加数据库 v11 维护任务表：指标归一化改为持久化后台任务，支持报告级进度、结果和错误记录，应用重启后可继续未完成任务。
- 增加数据库 v12 用户趋势置顶表：置顶配置按 fnOS 用户、家庭成员、归一化指标 Key 和单位隔离，不影响其他用户查看同一成员时的排序。
- 增加数据库 v13 趋势元数据：为每条指标归一化结果持久化标准分类和通俗说明；升级时仅从本地内置字典回填已有数据，不自动调用外部 AI。
- 增加数据库 v14 指标目录元数据：标记由 AI 高置信度创建的全局标准指标，支持后续跨机构复用和审计；迁移本身不调用外部 AI。
- 增加数据库 v15 指标字典运行时：核心与远程完整快照、当前生效状态、更新历史、体检分类物化表及持久化未命中名称池；支持管理员手动检查和安装 GitHub Pages 远程字典、失败记录与历史快照回滚，升级过程不自动联网。
- 增加数据库 v16 最终迁移：为已记录 v15、但使用过早期测试结构的数据库补齐指标目录与别名物化列，新增独立形态学发现表、AI 解析单元表和真实调用尝试表；完整 v15 可直接升级，已记录早期开发版 v16 的数据库也会在启动时幂等补齐。
- 增加数据库 v17 AI 提取候选追踪表：按任务保存本地确定性提取、AI 提取、冗余、忽略和待核对状态，以及页码、来源行哈希和命中字典，便于在不保存 OCR 正文或 AI 响应正文的前提下审计解析完整性。
- AI 报告整理新增结构化形态发现提取，独立保存器官、区域、侧别、发现类型、尺寸、扩展测量、形态描述、原报告分级、原文证据和置信度；报告详情可逐项查看并展开原文。
- 新增本地报告内容预分类和类型提示词路由：不增加 AI 调用即可识别体检、检验、影像、功能检查、病理、门诊、住院、处方、票据和疫苗；综合体检保持文档主类型，各页面仍按实际内容使用专属提取约束。
- 数据库 v16 补齐诊断、药品、诊疗操作、疫苗、费用汇总与费用明细领域表，以及解析单元路由记录；既有早期 v16 数据库会在启动时幂等补齐，不递增数据库版本。
- 报告详情新增诊断记录、用药记录、诊疗与操作、疫苗接种和费用信息区块；成员档案导出同步包含这些结构化事实。
- 报告详情中的诊断、用药、诊疗操作、疫苗和费用支持逐项新增、校对与删除；每项 OCR 证据可直接打开对应高清原页，所有人工操作写入中文用户审计日志。
- 数据库 v16 增加统一的报告专属章节表，保存体检套餐/总检结论、检验标本与方法、影像方式与增强信息、功能检查描述、病理肉眼/镜下/免疫组化/分级分期、门诊病史/体格检查/处置/医嘱，以及住院经过和出院医嘱；既有早期 v16 数据库启动时幂等补表，不递增版本。
- 报告详情新增“报告专属内容”区块，支持完整原文展示、人工新增/校对/软删除和对应高清原页定位；人工处理后的章节在 AI 重跑时保持优先，删除内容不会被同类 AI 结果重新恢复。
- 趋势区新增“形态变化”视图：按家庭成员、器官、侧别、区域和发现类型保守关联历次形态发现，展示尺寸、原报告状态、分级和描述变化，并可反向打开来源报告及对应单页高清图；歧义记录独立进入待确认，不强行合并。
- 形态变化补齐人工确认闭环：报告详情可逐项校对器官、区域、侧别、类型、尺寸、形态和原报告分级；待确认记录可关联已有变化线、建立独立变化线或标记为误提取，既有变化线支持点位拆分、合并及创建关联来源报告的复查提醒。
- 维护工具新增“重新关联历史形态发现”，纯本地同步重建历史关联并展示扫描、分组、未关联和歧义数量；AI 新整理完成的报告会自动更新当前成员的形态变化线。
- 跨设备恢复完整备份后，当前 fnOS 系统管理员会自动接管现有家庭成员的管理权限，旧用户和历史操作记录继续保留。
- 核心指标字典补齐 44 个常规体检高频项目，覆盖收缩压、舒张压、脉搏、臀围、完整血细胞参数、尿常规、肝肾功能、餐后/随机血糖和常见电解质；尿蛋白定性/定量、尿素/尿素氮使用独立标准 Key，避免不同值类型或换算关系混入同一趋势。
- “我的 → 重复报告检测”独立维护页：手动扫描当前成员候选，按组对比原件指纹、业务编号、OCR 文本和结构化指标差异；支持确认重复、标记不同、批量治理、撤销判断与合并原件页，合并后源报告自动移入回收站，扫描指标与人工治理记录全程留痕。
- 趋势数据节点原图证据定位：点击趋势折线节点打开来源高清原图，自动缩放定位到指标所在 OCR 行并以行组框高亮，描边样式不遮挡原文；触屏窄视口按行宽约束缩放并钳制平移，页面始终覆盖视口且标记行保持居中。
- 报告字段校对弹窗支持在原件上方叠加显示/隐藏 OCR 文本层，任意 OCR 行点击即复制并显示黑色 Toast 提示，PC 与触屏均可逐行取词。

### Changed

- AI 输入规划升级为 `ocr-unit-plan-v11`：不同内容路由不再共用固定页数硬切，指标单元按字符、候选数和预计输出量重新装箱，稀疏叙事内容跨原分片合并；字典唯一命中的可靠定量/定性表格行直接在本地提取，跨页续表可在重复表头后继承章节。当前 23 页体检样本由 9 个主单元收敛为 4 个，同时保留输出超限后的单元级自动拆分。
- 遗漏检查改为所有主单元完成并由服务端合并后统一生成候选清单；定量/定性指标与形态发现可在同一个 `verification` 补提取请求中分别输出并独立校验证据，只有统一候选超过安全预算时才继续拆分，不再因类型或单页边界产生零散调用。
- 远程指标字典默认采用 Gitee Raw 国内镜像优先、GitHub Pages 备用的多源下载策略；网络、HTTP 或完整性校验失败时自动切换下一来源，实际成功来源会展示在检查结果并写入更新快照。部署侧仍可通过环境变量覆盖单一来源或自定义多源顺序。
- AI 文档概况与统一遗漏核对提示词升级为 `health-record-routed-v6`：内部预分类枚举不再拼入提交给模型的 OCR 正文，避免 `checkup` 等路由值被模型复制为报告子类型或检查部位；综合体检同时明确省略不存在的单一科室、子类型和部位，并禁止把体检表格中的“主诉、既往史、体格检查”等同名项目误作门诊/住院章节；遗漏核对严格区分指标与形态发现输出。
- AI 输入脱敏补充无“姓名”标签的体检表格行识别，例如“姓名值｜性别｜年龄”版式会过滤姓名并保留性别和年龄；业务报告编号继续保留。
- 修正报告类型枚举误写入检查部位的问题：`checkup`、`laboratory` 等内部值不再作为部位保存，综合体检缺省显示为“综合体检”；无具体科室的综合体检使用同名展示兜底，既有 v16 错误数据会在启动时幂等修复。
- AI 报告整理的单个解析单元请求默认超时由 3 分钟提高到 10 分钟，并在任务执行期间持续续租；慢模型或高峰期请求不再因固定 180 秒限制过早失败，超时提示同时明确到当前解析单元。
- 指标维护页将容易混淆的“历史指标归一化”调整为“历史指标字典匹配”，操作明确区分“匹配未归类项”和“全部重新匹配”，并说明该功能不重新 OCR、不调用 AI、不能修复原始名称或数值提取错误。
- 重组“我的 → 维护工具”的指标相关信息架构：新增独立“指标管理”页面，集中承载指标字典、历史指标字典匹配和指标问题池；维护首页仅保留统一入口，字典与问题池页面返回时回到指标管理。
- 为适配飞牛应用中心上架与统一更新管理，移除“我的 → 关于”的 GitHub 版本检查入口及服务端更新检测接口；应用版本更新由飞牛应用中心负责，远程指标字典更新能力保持独立可用。
- AI 输入规划升级为 `ocr-unit-plan-v9`：OCR 行先按元数据、表头、章节、指标、形态、叙事和噪声分类；没有指标候选的住院经过、出院医嘱、门诊病史、病理所见和体检总检章节不再被跳过，原始 OCR 数据保持不变。
- AI 提示词升级为 `health-record-routed-v3`：文档概况、定量/定性指标、形态发现和原文章节使用独立最小输出契约，不再为每个单元重复发送全部领域 Schema，也不再在 OCR 正文后重复追加候选原文。
- 长报告新增跨页文档概况路由，按总检/出院/病理结论、元数据、章节目录和首页内容的优先级生成标题及公共字段；未超限的单页报告继续合并文档与指标提取，超大单页保持独立完整指标单元。
- 表头清晰、指标字典唯一命中且 OCR 置信度可靠的表格行改由服务端直接形成高可信结构化指标，并从常规 AI 指标单元和遗漏补提取中移除；AI 结果仍执行证据校验，本地事实在最终事务合并时保持优先。
- AI Provider 新增 OpenAI 与豆包（火山方舟）配置模板，支持独立保存 API 地址、API Key、文本模型和视觉模型；切换 Provider 或为不同 AI 场景绑定模型时不会丢失已有配置。豆包兼容方舟模型名称及 `ep-...` 推理接入点 ID。
- 为报告机构关联增加 `reports(organization_id, report_issued_at DESC)` 索引，支撑后续按本地归并机构筛选和时间排序；已有 v16 数据库启动时幂等补齐，不递增数据库版本。
- 报告结构化整理已迁移到统一 AI 任务接口，继续沿用现有分单元提取、动态 Token、证据校验、审计和重试行为；通用 AI Runtime 不包含医疗提示词或报告写入逻辑。
- AI 提取提示词升级为 `health-record-routed-v1`：公共约束按解析单元叠加最多两个类型插件，分类失败自动回退通用提取且不丢页；只有定量/定性指标进入趋势，诊断、用药、操作、疫苗和票据事实分别落入专属结构。
- 专属事实采用人工整项保护：人工新增或修改后不再被 AI 重跑覆盖，人工删除会保留本地抑制标记，同一 OCR 证据后续不会被模型重新写回；未人工处理的 AI 记录仍随完整重跑原子更新。
- 类型提示词升级为 `health-record-routed-v2`，通过固定 `sectionKey` 输出报告专属章节；服务端只接受白名单章节键、非空原文和当前解析单元可验证的 OCR 证据，跨单元同类章节会去重合并后再与其他结果原子写入。
- AI 形态发现输出契约升级至 v4：只允许输出可独立命名、可跨报告跟踪并带原文证据的具体发现；检查标题、占位名称、正常解剖描述和无具体对象的阴性结论均被明确禁止，并由服务端再次强制验收。已有历史噪声也不再进入形态变化或待确认列表；明确点名结节、囊肿等对象的阴性记录仍会保留。待确认卡片同时展示来源报告标题和原文，避免用户仅凭 AI 生成名称判断归属。
- AI 结构化请求不再固定使用 8,192 输出 Token：现在按主提取、格式重试或遗漏补提取任务类型，以及页数、输入字符、候选指标和形态发现复杂度动态计算预算，并受当前 Provider 模型能力上限约束。模型因长度截断时先扩大当前单元输出预算，仍超限才递归拆分该单元，已完成单元和其他页面不会重复调用。
- AI 报告整理改为逐解析单元调用：每个单元独立保存输入哈希、状态、结果、Token、耗时和尝试历史，父任务重试时复用已完成单元；全部主单元成功后才合并公共字段、指标和形态发现，并在单个事务中替换旧 AI 指标，避免长报告截断或半成品覆盖。
- AI 提取前先使用当前生效的核心与远程指标字典扫描 OCR 行，建立带标准指标候选、原始行和页码的本地事实清单；候选只帮助模型定位，最终结果仍必须引用实际 OCR 原文。
- OCR 页面重建开始使用识别坐标恢复阅读顺序，并将同一视觉行的项目、结果、单位和参考范围合并为稳定表格行；同时兼容 PDF 矩形坐标和 RapidOCR 四点坐标，原始 OCR 行 ID 会随重建结果保留。
- 定量/定性指标与形态发现改为独立解析路由：指标单元只接受公共字段和 observations，形态单元只接受 morphologyFindings；遗漏补提取和输出超限拆分继续保持原路由，避免影像所见混入趋势。
- AI 返回的每条指标和形态发现会反向匹配当前解析单元实际发送的 OCR 行，证据统一替换为精确 OCR 原文；无法验证或跨路由注入的结果不会落库、不会进入趋势，并计入解析质量警告。
- AI 返回无效 JSON 时在当前单元进行一次严格格式重试；网络、429 和服务端错误继续交由父任务重试，已完成单元不会重复调用。服务端不再依赖模型自报页面覆盖率，因此新任务不再产生 `AI_INCOMPLETE_COVERAGE`。
- AI 合并后会确定性补齐 OCR 明确出现的身高、体重、BMI、腰围、臀围、脉搏、收缩压和舒张压；其他可信但未匹配的数据行只按页补提取一次，仍无法确认时报告正常完成并显示待核对数量，不反复重跑整份报告。
- AI 审计中的调用次数按真实解析单元尝试统计，报告详情处理进度展示已完成单元、当前页码、遗漏补提取和待核对数量。
- AI 主解析和遗漏补提取默认最多同时执行 3 个单元；任一主单元失败后停止领取新单元，但会等待已启动调用完成并保存结果，父任务重试时只补失败单元。遗漏候选会按完整页面块重新装箱，减少逐页调用产生的提示词和延迟。
- AI 单元结果合并增加 OCR 来源级去重：同一 OCR 行、同一指标语义和同一结果即使被模型返回为不同名称或字段完整度不同，也只保留一项并合并参考范围、单位和证据；同一行中的不同指标继续保持独立。
- AI 指标最终落库增加报告级去重：同一报告跨页重复出现的标准指标在单位兼容且结果相同时合并为一条，并保留汇总页、明细页的全部证据；明确属于历史结果、不同采样时间、标本或检测方法冲突的记录继续独立保存。
- AI 输入在脱敏后、单元规划前增加保守去噪：删除页码、打印声明、纯脱敏占位符和低置信短噪声，跨页重复内容仅清理页面首尾区域的短页眉页脚；表头、章节、指标、形态内容和正文中的重复医学描述不会按频率删除。报告级输入计划同步返回原始、移除和实际发送行数。
- AI 指标输出改为省略空字段的紧凑对象，项目名称继续保留原文并由本地字典归一化；公共字段只返回当前单元明确出现的内容，不再要求每个单元输出完整空结构或生成分片摘要。截断失败会保留模型返回的实际输入、输出 Token 和耗时，便于定位 Provider 输出上限。
- 指标归一化改为只使用当前核心与远程字典：报告处理、增量整理和全量重跑均不再调用 AI 创建指标或别名；未命中名称统一进入问题池，字典更新后仅在本地回填历史指标。
- 字典版本回填会清理未被当前字典接纳的历史 AI 指标关联，回收站报告不参与自动回填或全量归一化。
- 内置指标和趋势分类的运行时来源由分散的 TypeScript 常量统一为项目内核心 JSON，当前 115 个定量/定性指标集中维护别名、单位、说明及体检目录排序；形态学发现继续保留在报告所见而不进入指标字典，发布校验会阻止重复 Key、无效分类引用、错误类型组合和远程核心 Key 重定义。
- 应用启动时将随包核心字典物化到 SQLite；趋势、搜索和指标归一化统一读取当前生效的核心与远程目录。远程文件完成 HTTPS、大小、SHA-256、JSON Schema 和可选 Ed25519 签名验证后才会在单个事务中切换，失败不影响当前版本。
- 指标问题池改为按观察项持久化未命中字典的名称、单位和章节，同一观察项重复整理不会增加次数，报告进入回收站或后续命中字典时自动移出；核心或远程 revision 变化后由后台执行纯本地增量回填，不重新 OCR、不调用 AI。
- 本地打包和 GitHub Actions 发布的 fnOS 安装包统一增加应用版本号，文件名格式为 `fnos-app-health-records-<version>.fpk`，避免下载和保留多个版本时混淆。
- 应用运行日志改为写入磁盘前统一递归脱敏，覆盖 API Key、认证信息、密码、手机号、身份证号、邮箱、设备路径和报告正文类字段；页面读取保留二次脱敏以兼容旧日志。
- AI 报告提取提示词升级至 v5：体检报告的一般检查、基础测量和体格检查会逐项提取身高、体重、BMI、腰围、臀围、脉搏和血压等明确测量值；`120/80 mmHg` 必须拆成收缩压、舒张压两项，不合并项目，也不推算原报告缺失的数据。囊肿、结节、斑块、息肉、结石、积液、器官形态、回声、血流和影像分级等内容统一进入形态发现，不再混入趋势指标。
- 对历史未归一化观察项增加一次性本地形态迁移：只迁出明确的形态内容，不调用 AI，也不移动已经命中字典的定量或定性指标；重复启动保持幂等。
- 形态变化采用确定性追踪规则：同一机构与否不影响关联，左右侧冲突绝不合并；同一器官存在多个区域时，缺少区域的记录保持待确认。变化摘要只复述原报告尺寸、状态、分级或描述差异，不生成恶化、恶性、疾病风险或治疗建议。
- 形态人工校对和人工变化线使用字段级保护；自动重建不会覆盖人工分组，重新 OCR+AI 时只更新未人工修改的字段，误提取和无法唯一匹配的人工记录不会被静默删除。
- 指标字典版本升级后由后台维护任务自动增量回填旧报告的归一化结果，已存在的历史指标会自动合并到统一趋势，无需管理员手动运行“整理未归类指标”；该过程不重新 OCR、不调用 AI，未命中的旧 AI 关联会转入问题池。
- “增量整理”和“全量重跑”改为提交后台任务后立即返回，维护页仅在任务执行期间轮询进度，离开页面不会中断处理，也不再受网关 5 分钟请求超时限制。
- 趋势页按常见体检报告目录展示“一般检查、内科、外科、检验、功能、影像”等标准分组，检验项目进一步按血常规、尿常规、肝肾功能、血脂、血糖等固定子分组排序；报告日期只决定单个指标内部的数据点顺序。
- 趋势页增加“需要关注”区域，仅根据最新去重数据点的原报告异常标记、可靠参考范围和 10% 边界规则展示“报告已标异常”或“接近参考边界”，不生成疾病判断或医学预测。
- 趋势页增加固定指标状态筛选，可查看全部状态、全部需要关注、报告已标异常、接近参考边界和未列入关注的指标；触屏端筛选控件分两行排列，避免横向溢出。
- 压缩趋势页信息密度：关注明细默认收起，未启用筛选时隐藏结果摘要，并收紧筛选器、分组标题和趋势卡片间距。
- 档案页搜索栏同步采用紧凑尺寸，收紧桌面筛选行和触屏粘性搜索区间距，不改变搜索、分页和下拉刷新行为。
- 趋势指标“整理详情”提升为视口级弹层，根据按钮上下可用空间自动选择展开方向，内容过长时在弹层内滚动，避免 PC 和飞牛 App WebView 中被卡片或视口裁剪。
- 指标字典补充白细胞数目、白细胞数目（WBC）等白细胞计数别名，并新增“中性粒细胞百分比”标准指标，统一中性粒细胞比率、百分比、百分率及 NEUT% 等机构名称；字典版本升级后会在本地自动合并历史趋势，不重新 OCR 或调用 AI。
- 指标名称解析支持拆分前置、后置和括号内英文代码，所有机构共用同一全局指标目录；百分比/绝对值、空腹/餐后、高切/低切继续严格分开，单位不兼容时拒绝误合并。
- 内置字典回填和管理员全量重新归一化会在新旧系列映射唯一时迁移当前用户各自的趋势置顶；一对多拆分不自动迁移，避免误置顶。
- 趋势卡片右上角保留置顶/取消置顶按钮；当前用户置顶的指标进入独立“我的关注”分组，并继续按 fnOS 用户和家庭成员隔离。
- 趋势搜索仅匹配标准指标名和已启用的可信字典别名，不再使用历史来源名称、单位或章节内容命中；分组筛选改为服务端返回的标准体检分组。
- 趋势折线增加可点击的数据节点，节点上方显示数值、下方显示报告日期；高于参考范围的节点以红色标识，数据较多时图表支持横向滚动，触屏端可直接点击节点查看对应原图或报告。
- 趋势指标“整理详情”按钮并入最新值行右对齐，不再独占一行挤压卡片空间；弹层行为保持不变。

### Fixed

- 修复趋势卡片没有变化区间或日期范围时，“整理详情”按钮失去右侧锚点并掉到左侧的问题；按钮现在始终与卡片右边缘对齐。
- 修复综合体检的科室表格因包含“主诉、既往史、体格检查”等标签，被局部分类误路由为门诊病史并展示重复原文的问题；整份文档类型现在约束提示词与服务端落库验收，既有 v16 错误 AI 章节会幂等清理，人工内容不受影响。
- 修复上传页在任务全部完成后仍永久保留旧处理面板，报告删除后切回页面又轮询旧报告并提示 404 的问题；成功任务离开上传页后自动清理，失败任务继续保留供重试，被取消或已删除的报告不会继续轮询。
- 修复报告移入回收站后，再进入“我的 → 回收站”仍显示缓存数据、必须刷新整个应用才能看到的问题；缓存页面重新激活时会自动重载第一页。
- 修复 AI 审计页被页面缓存后只在首次进入时读取数据，导致先打开空审计页、再完成报告整理并返回时仍显示“暂无 AI 调用记录”的问题；现在切回页面和应用数据变化后会自动静默刷新。
- AI 整理详细日志改为按解析计划固定排序的一单元一行视图，集中显示整体完成状态、单元页码和等待/处理/完成/待核对/失败状态；并发调用产生的格式重试、输出扩容、拆分和失败历史默认收进对应单元的可展开执行记录，不再堆叠成大量进度节点。
- 修复 Gitee Raw 返回官方 `raw.giteeusercontent.com` 302 地址时被下载器当作网络错误，以及远程指标引用核心分类时被运行时误判为分类不存在的问题；下载器现在仅允许 HTTPS 同源跳转和 Gitee 官方 Raw 跨域跳转，其他跨域跳转继续拒绝。
- 远程指标字典补齐三项常见甲状腺抗体及其中英文别名：抗甲状腺过氧化物酶抗体、抗甲状腺球蛋白抗体和促甲状腺激素受体抗体；英文大小写、空格、连字符和下划线写法统一命中同一标准指标，`kIU/L` 与等值的 `IU/mL` 不再拆分趋势。
- 修复报告只有执行/报告/开单科室而没有就诊科室时，档案卡片能显示科室但详情弹窗显示“待整理”的问题；详情展示统一使用有效科室，校对表单仍保留各科室原始语义。
- 修复“查看全部指标”弹窗层级被通用弹层样式覆盖、可能显示在报告详情下方的问题。
- 修复报告详情的 AI 整理详细日志只在打开弹窗时加载一次的问题：任务排队或处理中每 2 秒自动刷新，完成后获取最终记录并停止轮询；关闭弹窗、切换报告和页面失活时同步释放轮询。
- 补齐用户操作日志的指标字典同步、远程更新和回滚中文业务名称，并为指标字典、用户账号、未知动作及未知对象提供中文化展示；字典版本、指标数和别名数改为可读摘要，已删除或无法解析的业务对象也不再暴露英文内部标识和 ID。
- 修复 BMI 被错误写入体重趋势的问题：基础测量兜底不再把“体重指数”前缀识别为“体重”，`kg/m²` 不再被截断成 `kg`；AI 缩短项目名时优先恢复 OCR 表格首列的完整项目名称。
- 修复 Kimi/Kimi Coding 模型连接测试与正式解析请求被上游拒绝的问题：Kimi 请求统一使用服务要求的 `temperature: 1`，并将常见误填的 `https://api.kimi.com/coding` 自动规范为 `/coding/v1`；旧配置读取时立即兼容，无需重新填写 API Key。
- 修复模型引用完整表格行时仍可能把历史结果列写成本次值的问题：证据校验现在对所有明确表格行以第一结果列为准，并同步采用 OCR 最近章节纠正模型返回的错误章节。
- 修复左右侧 ABI、baPWV 被遗漏或合并为同一趋势的问题：核心字典增加四个侧别标准指标，本地会从双侧明确数值中确定性补齐；模型从同一双侧证据生成的无侧别单值会被移除，只有原报告确实没有侧别时才保留通用指标。
- 修复形态汇总页使用“右侧”、明细页使用具体解剖区域时同一病灶无法合并的问题：纯侧别不再作为区域冲突，侧别仍由独立字段严格约束，不同尺寸病灶继续分别保留。
- 远程指标字典 revision 2 补齐 AFP、CEA、CA19-9、CA15-3、CA242、CA50、NSE、CA72-4 和 CYFRA21-1 等真实体检报告中的常见肿瘤标志物，更新字典后可纯本地回填历史趋势。
- 修复综合体检报告中本次结果、参考范围和历史结果列混淆的问题：支持 `【尿常规15项】`、`【便常规】` 等括号章节及跨页章节继承；本次结果为 `-`、`±` 时不再把参考数值写成结果，历史检查章节不再进入本次指标或形态发现。
- 修复体检汇总与专项明细重复生成形态记录的问题：同报告内按标准器官、侧别、病变类型和尺寸语义合并摘要与明细，保留全部原页证据；明确存在不同尺寸的多个病灶仍分别保存。
- 修复便常规白/红细胞误归入血常规、心电图心率与一般脉搏混成同一趋势的问题；核心字典补充尿镜检、心电图、ABI、baPWV、PSA 及常见肝肾功能全称别名，并使用章节和标本硬约束消除同名歧义。
- 修复体检报告生成日期依赖 AI 且可能缺失的问题：服务端优先从已过滤历史内容的 OCR 中确定性读取终检时间、报告时间和检查日期，人工校对字段继续保持最高优先级。
- 修复 ECG 增益/走速、呼气试验试剂信息、血管图表刻度和“正常范围/未见异常”解释性文本被当成指标候选的问题，减少无效补提取和异常指标入库。
- 修复长体检报告的标题、报告类型和机构被末尾专项检查页覆盖的问题：只有首个标量单元可提取文档级公共字段，后续标量单元只提指标、形态单元只提形态发现；服务端会丢弃越权字段。单页检验单仍作为唯一文档锚点完整提取标题、类型、机构和日期。
- 修复长报告 AI 调用次数和无效指标偏多的问题：目录、业务编号、采样/报告时间、单独参考值、图表刻度及健康宣教正文不再进入指标候选；“高/低密度脂蛋白”不会误触发形态提取，形态页会跨标量分片集中装箱。当前 23 页测试报告由历史 19 次真实调用收敛为 5 个主单元。
- 修复历史结果列中的异常箭头污染本次指标状态、指标章节普遍为空的问题：异常标记只读取本次结果单元格，本次数值在参考范围内时不会沿用历史高低标记；已验证证据会向上关联最近章节。报告任务详情同时改为汇总全部真实 AI 尝试的成功、失败、Token 和耗时。
- 修复重复报告提示在已有报告中反向出现或切换报告时短暂串到其他详情的问题：重复匹配仍保持双向供查重维护使用，但详情警告只在当前待确认报告中展示；详情、摘要和异步响应均按当前报告 ID 隔离。
- 修复指标单位只出现在结果文本、结构化 `unit` 为空时，同名指标被拆成多个趋势卡片的问题；趋势会保守识别结果值后的常见单位，再参与分组和重复点合并。
- 修复同一份原件因多次 AI 提取生成不同标题、报告类型或检查部位而漏过查重的问题：同成员下上传原件指纹完全一致时直接标记为高可信重复；原件不同则按归一化指标键和数值计算内容重合，兼容结果文本、单位拼接及部分指标漏提取差异。
- 修复同一机构因地区、院区前缀或简称差异而漏过重复检测的问题：机构名称近似时必须同时满足同日期、同报告类型和核心指标高度重合，趋势页也会据此合并重复数据点，同时避免仅凭名称相似误判不同报告。
- 外部备份选择改由覆盖按钮区域的原生文件输入直接触发，不再调用隐藏输入框 `.click()`，修复部分飞牛 App WebView 点击“上传恢复”无响应。
- 修复历史报告被误判为“未识别到文字”的问题：text_length 字段加入前的旧 OCR 记录该值为空，导致正常旧档案错误显示空识别警告、手动 AI 整理也被错误拦截；现在按 OCR 文本行折算有效长度，新数据行为不变。
- 修复在档案页连续校对多份报告时，后打开报告可能沿用上一份报告 OCR 文本的问题：OCR 缓存现在绑定报告 ID，切换报告会立即清理旧状态并阻止过期请求回写；处理进度轮询同步增加报告隔离，避免慢请求造成状态串线。
- PDF 拆页现在校验 Worker 声明页数、页面明细和数据库分页记录必须连续一致；AI 整理前再次校验 PDF 页面集合与每页 OCR 结果，避免缺页数据被静默送入模型。
- 修复部分 WebView（纯血鸿蒙卓易通等）fetch 上传 FormData 直接失败的问题：上传请求自动降级 XMLHttpRequest；网关返回 HTML 错误页、fetch 英文技术报错统一转为可理解的中文提示并保留 HTTP 状态供排查。
- 修复原图查看器翻页时 OCR 标记先于原稿显示、原稿区域缺少占位高度导致内容抖动堆积的问题；编辑原件按钮并入 OCR 开关行右对齐。
- 修复报告字段校对弹窗“显示OCR”开关点击无效的问题。
- 修复 Toast 提示因 `left: 50%` 居中使 shrink-to-fit 可用宽度被压缩、12 字以上多一字即换行的问题；Toast 统一为内容自适应宽度，内容超过屏宽 90% 才换行。

## 0.1.13 - Unreleased

### Added

- OCR 未识别到任何文字时给出明确提醒：上传页处理结果区和报告详情标题下直接显示“可能不是有效的体检报告”警告，提醒页新增琥珀色“需要注意”通知，引导重新上传清晰原件或手动录入。
- 新增全局服务端错误兜底：未预期异常（代码缺陷、依赖异常）写入应用日志并保留完整调用链堆栈，对外只返回通用提示，不再泄露内部细节。

### Changed

- OCR 全文为空的报告不再自动进入 AI 整理，手动触发 AI 整理也会提前拦截，避免空内容任务排队、重试后失败。
- 成员清单导出和备份文件下载改为应用内校验后再保存：失败时在页面内提示原因，不再跳转到裸 JSON 错误页丢失当前状态。
- 报告处理进度轮询和 OCR 安装状态轮询允许偶发网络失败，连续多次失败才停止并提示，避免进度条假死、界面卡在“正在安装”。
- 图片查看器加载原图失败时显示“图片加载失败”并提供重试按钮，不再一直转圈或显示破图。

### Fixed

- 修复 HTTP 内网访问（非 HTTPS）选择文件后完全无反应、不产生任何档案记录的问题：`crypto.randomUUID` 在非安全上下文不可用导致入队中断，已做降级处理，入队过程增加异常兜底提示；遗留本地登录 cookie 的 Secure 标记改为按请求实际协议下发。
- 修复服务端中文错误提示无法显示的问题：框架升级后错误格式变化，界面只显示“请求失败（HTTP xxx）”，现已正确解析，确认归档失败、重复操作、权限不足等业务错误恢复显示真实原因。
- 修复断网、网关异常时提示不友好的问题：无法连接服务器、返回非 JSON 错误页等场景现在显示可理解的中文说明，并附带技术细节和 HTTP 状态码便于反馈定位。
- 修复确认归档、移入回收站、AI 整理、重新识别、任务重试失败时错误只显示在默认折叠的处理进度区、用户毫无感知的问题：现在会自动展开该区并弹出提示。
- 修复下拉刷新和点击刷新在加载失败时仍提示“已刷新/已是最新数据”的误导（概览、提醒、AI 审计、用户操作日志）。
- 修复隐私模式等 localStorage 不可用环境下应用白屏的问题：主题选择和成员选择降级为不持久化，功能不受影响。
- 修复概览、关于页面加载失败缺少重试入口的问题；备份操作错误改用红色错误条展示，不再与灰色说明文字混淆。
- 补充全局未捕获异常兜底：任何漏网的同步或异步异常都会在控制台留痕并提示“操作未生效，请重试”。

## 0.1.12 - Unreleased

### Added

- 维护工具新增“全量重跑”：清空已有指标归一化结果并全部重建，用于修复历史误并的指标系列；
- AI 指标归一化提示词升级：括号内的测量条件（如 高切/低切/中切、空腹/餐后）必须保留，属于不同指标不得合并。

### Changed

- 页面切换启用 KeepAlive 缓存：切回已访问页面立即渲染缓存内容，不再重新加载和闪烁，并保留滚动位置；上传完成、后台 OCR/AI 任务跑完等数据变化会静默刷新当前页面。
- 报告处理中的接口轮询优化：OCR 状态不再跟随每轮轮询，详情/列表/提醒级联刷新增加 10 秒节流，稳态每轮仅 1 个任务状态请求。
- OCR 首次安装的 pip 默认镜像源改为清华大学镜像，降低国内设备安装耗时（不影响已保存的镜像源设置）。
- 结构化指标行重构：指标名称独占一行不再截断，参考范围与检查分组同行两端对齐，修复触屏端名称和参考值被折叠的问题。
- 趋势点和报告详情在原报告无异常标记时不再显示“未标记”。
- 异常标记补齐：AI 漏标时基于原文中的 ↑↓/偏高/偏低 标记在提取和读取两侧确定性兜底，存量报告无需重跑即可生效。
- 用户操作日志中的提醒/通知/报告状态值中文化展示。
- 指标数值展示去重：结果文本已包含单位时不再重复拼接单位（如 “89 mmHg mmHg”）。

### Fixed

- 修复趋势系列把不同切变率血粘度误并为一条的问题：AI 归一化键不再剥离括号内容，不同测量条件生成独立系列。
- 修复同一报告重复上传（已归档/待确认多份并存）在趋势中重复计点的问题：完全相同的数据点只保留一份，优先已归档和归一化质量更高的来源。
- 修复多个页面加载失败被伪装成空数据的问题：概览、档案、趋势、AI 配置、运行状态增加错误提示和重试入口；应用启动失败不再静默落到登录页。
- 修复提醒状态变更、回收站恢复、重复报告合并/移入回收站等操作无加载态、失败无提示的问题。
- 修复回收站忽略分页导致超过一页的报告无法恢复或永久删除的问题，并补充“加载更多”。

## 0.1.11

### Added

- 初始化健康档案 fnOS Native 应用。
- 增加概览、档案时间轴、趋势、上传、提醒和“我的”响应式工作台。
- 增加 SQLite 健康档案领域 Schema、WAL、版本化迁移和数据库运行状态。
- 增加应用版本升级记录与 SQLite 按需迁移规范。
- 增加拍照、多图、HEIC、JPEG、PNG、WebP 和多页 PDF 上传。
- 增加 PDF 拆页、单页高清图、原件查看、缩放、翻页、全屏和下载。
- 增加 OCR、PDF、AI Provider、多模型配置、AI Key 加密和异步任务处理。
- 增加处理进度、详细日志、失败重试、手动 AI 整理和单篇报告重新 OCR+AI。
- 增加报告校对、确认归档、移入回收站、恢复和永久删除。
- 增加成员管理、成员授权、重复报告检测、趋势来源反向定位、提醒和任务通知。
- 增加高频检验指标内置字典、指标归一化结果、趋势可信度说明、指标理解问题池和历史指标重新归一化维护工具。
- 扩展体检基础测量、妇科超声、感染筛查和常见体检阳性发现字典，减少指标问题池中的未命中项。
- 增加非预设指标 AI 兜底归一化：内置字典未命中时，可调用已配置文本模型生成标准名、类型和趋势建议。
- 增加 OCR 文本质量评分、弱识别原因记录和报告详情提示。
- 增加 PDF 文字层与高清渲染 OCR 合并策略，减少清晰 PDF 因文字层不完整导致的漏识别。
- 增加报告字段人工校对保护，AI 重跑和重新 OCR+AI 不再覆盖用户手动补全的公共字段。
- 增加完整应用备份创建、列表、下载、校验、上传外部备份恢复、恢复前安全备份和备份记录删除。
- 增加用户操作日志、AI 审计、运行与识别状态、维护工具和关于页面。
- 增加 fnOS 网关身份、系统管理员权限和可配置 HTTP 服务端口。
- 增加 GitHub Release notes 生成脚本，发布页会读取当前版本 changelog 段落展示本版本变更。

### Changed

- 优化“备份与恢复”页面：备份操作独立为操作区，长备份文件名会在溢出时循环展示，便于在触屏端核对完整文件名。
- 报告字段校对支持报告生成时间与检查时间的秒级查看和编辑，并保持原有时间精度回填与保存。
- 统一触屏端弹窗和抽屉的标题栏布局，长标题会省略显示并保留关闭入口。

### Fixed

- 修复报告字段校对弹窗在触屏端滚动后标题栏消失的问题；成员管理、OCR 文本、任务详细日志等同类弹窗同步固定标题栏。
- 修复触屏端校对表单中输入框、日期时间控件和下拉选择器宽度不一致、内容未垂直居中的问题。
