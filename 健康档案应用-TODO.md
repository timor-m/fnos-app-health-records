# 健康档案应用 TODO

本文档是健康档案的实施清单。只有实现、测试和必要的真机验证全部完成后，任务才能标记为完成。

## P0 项目框架

- [x] 项目元数据、依赖、Git 忽略规则和 CI。
- [x] Vue Router、PC 侧栏、触屏底栏及档案、趋势、上传、提醒、设置页面骨架。
- [x] SQLite WAL、版本化迁移、数据库中间件和测试环境。
- [x] fnOS 网关身份和统一请求用户模型，fnOS 系统管理员即应用管理员。
- [x] 安装/配置向导仅配置服务端口，默认 3334。
- [x] Unix Socket 与 HTTP 服务端口双监听、端口冲突检测和配置回滚。
- [x] RapidOCR/OpenVINO、PyMuPDF Worker、运行环境安装和任务队列骨架。
- [x] OpenAI-compatible 文本/视觉 Provider、API Key 加密和连接测试骨架。
- [x] fnOS 构建、`.fpk` 打包和包结构校验。
- [x] 使用正式的健康档案应用图标替换模板占位图标。

## P1 核心档案闭环

- [x] 家庭成员档案、默认本人和基础资料。
- [ ] 成员头像上传、裁剪和删除。
- [x] fnOS 系统管理员成员管理，以及 fnOS 账号的查看、管理两级授权。
- [x] 拍照、多图、拖放、HEIC、JPEG、PNG、WebP 和多页 PDF 上传。
- [x] 上传前页面旋转、排序、移除、图片缩略预览和原件保存。
- [x] 多页 PDF 服务端拆分、逐页缩略图/OCR 任务生成和结果持久化。
- [x] 已上传页面旋转、排序、删除和缩略图重新生成。
- [ ] 已上传报告按页面拆分成多份报告。
- [x] PDF 原生文字提取、扫描页渲染回退和逐行 OCR 坐标持久化。
- [x] AI 提取前按 OCR 行顺序重建页面，脱敏并保守清理重复页眉页脚、页码和打印噪声，按最多 8 个完整页面及 45 个候选自适应装箱且不拆分单页内容，并生成输入哈希。
- [ ] 跨行字段、跨页表格关系和复杂版面语义重建。
- [x] 报告级文本汇总、敏感身份信息过滤和 OpenAI-compatible AI 结构化提取。
- [x] AI 解析单元最多 3 路并发调用、状态持久化、断点恢复、基础测量兜底、遗漏补提取、OCR 来源级指标去重和最终原子合并。
- [x] AI 字段证据、置信度、模型、处理版本、Token 和耗时持久化。
- [x] 形态学发现独立提取和持久化，覆盖器官、区域、侧别、尺寸、形态描述、分级、原文证据，并与趋势指标强制分流。
- [x] 诊断、用药、诊疗操作、疫苗和费用独立落库、报告详情展示、证据原页定位、人工增删改、操作审计及 AI 重跑整项保护。
- [x] 报告详情人工校对界面和字段更新审计。
- [ ] 字段级修改历史和人工值优先合并策略。
- [ ] 可信报告自动入档，缺失日期、字段冲突和混合报告进入待确认。
- [x] 按医院报告生成日期排序的时间轴和每页 30 条游标分页。
- [x] PC 分栏详情、触屏详情弹窗、原件缩放、翻页、全屏和下载。
- [x] 报告编辑、移入回收站、30 天保留、恢复和永久清理。
- [x] 上传后离开页面继续处理、任务状态、任务租约、自动重试和失败任务人工重试。
- [ ] 上传队列草稿保存和页面刷新后恢复。

## P1 报告公共字段

- [x] 医院原名、院区/分院和城市提取。
- [ ] 标准医院、体检机构及医院别名归一化。
- [x] 就诊场景、就诊科室、开单科室、执行科室、报告科室和住院病区。
- [x] 检查部位原文、标准部位、层级部位、左侧/右侧/双侧。
- [x] 报告号、门诊号、住院号、体检号、检查号、标本号和条码号。
- [x] 报告生成、检查、申请、采样、接收、审核、入院和出院时间。
- [x] 开单、检查、报告、审核和总检医生。
- [x] 临床诊断原文、检查目的、主诉、检查所见、结论和明确复查要求。
- [x] 字段来源、置信度、页码、OCR 坐标、模型和处理版本。
- [x] 身份证、电话、住址不得进入结构化字段、搜索索引、日志或 AI 摘要。

## P1 分类专属字段

- [x] 体检：套餐、基础测量、科室分项、阳性发现、异常汇总、总检结论和原报告建议。
- [x] 检验：标本、项目编码、结果、单位、参考范围、异常标记和方法。
- [x] 影像：检查方式、部位、左右侧、增强信息、检查所见和影像结论。
- [x] 影像/超声/内镜形态发现：病灶类型、器官部位、侧别、尺寸、扩展测量、关键描述、原报告分级和原文证据。
- [x] 功能检查：心电图、肺功能、内镜等测量、描述和结论。
- [x] 病理：标本、肉眼所见、镜下所见、免疫组化、诊断及明确分级分期。
- [x] 门诊：主诉、病史、体格检查、诊断、处置和医嘱。
- [x] 住院：入出院信息、诊断、手术操作、住院经过、出院用药和医嘱。
- [x] 处方：药名、规格、剂型、剂量、频次、途径、疗程和数量。
- [x] 票据：票据号、总额、医保、自费、费用分类和明细。
- [x] 疫苗：名称、剂次、厂家、批号、接种日期、部位和下次日期。
- [ ] 其他：通用章节和键值事实，以及人工调整报告类型。

## P2 完整辅助功能

- [ ] 成员、医院、院区、科室、部位、类型、日期和异常状态筛选。
- [x] OCR 全文搜索及中文子串检索回退。
- [ ] 医院、科室和部位别名合并的数据字典。
- [x] 核心与远程指标字典、标准分类、通俗说明、趋势可信度、来源名称、未命中问题池、独立字典更新和历史指标本地重新归一化。
- [x] 同成员、同指标、兼容单位的趋势图，按常见体检目录和检验子分组排序，并支持来源报告和单页原图反向定位。
- [x] 同成员形态变化追踪，按器官、侧别、区域和发现类型保守关联，展示中性变化摘要、待确认记录，并支持来源报告和单页原图反向定位。
- [x] 形态待确认处理闭环：字段级人工校对、关联/拆分/合并变化线、误提取忽略、AI 重跑保护、操作审计和复查提醒。
- [ ] 后续增强：同器官、同侧、同类型多病灶缺少稳定亚区域时，支持人工候选关联及“新发/未再描述/明确未见”状态治理；保持保守不自动按尺寸续接，待真实家庭报告高频出现该需求后再实施。
- [x] 趋势“需要关注”聚合报告已标异常与接近参考边界指标，不生成疾病判断或医学预测。
- [x] 趋势按用户和成员隔离置顶，可信标准名/别名搜索，标准体检分组筛选和分组折叠。
- [x] 手工提醒和报告明确复查要求生成的待确认提醒。
- [x] 应用内提醒、任务通知、已读归档、到期/逾期列表和“我的”入口角标。
- [x] 内容重复报告检测入口、手动扫描、合并到已有报告和移入回收站操作。
- [ ] 精确重复文件检测。
- [x] 存储统计和后台任务统计。
- [ ] 成员报告 ZIP 导出。
- [x] 完整备份创建、列表、下载、校验清单、手动校验、上传外部备份恢复、恢复前安全备份和备份记录删除。
- [x] 原件查看、导出、删除、授权、配置、备份和登录失败审计。
- [x] AI 调用次数、耗时、Token 和失败状态审计。
- [ ] 视觉发送审计明细。
- [x] AI Provider 增加 Ollama 预设：继续使用 OpenAI-compatible `/v1/chat/completions`，支持可编辑服务地址、免 API Key、手动填写文本模型、连接测试和独立配置保存；文本连接测试同时验证报告整理依赖的 JSON 输出能力。
- [x] AI Provider 增加 MiniMax 预设：提供中国大陆 OpenAI-compatible 默认地址和 `MiniMax-M2.7` 文本模型，适配推理内容、输出参数和结构化结果测试，并禁止当前 M2 系列误启用视觉增强。
- [ ] Ollama 预设补齐 fnOS 同机、Docker 访问宿主机和局域网其他设备的地址说明，并验证长报告 JSON 输出、指标遗漏、上下文长度、输出截断、请求超时及服务不可达提示。
- [ ] Ollama 后续增强：通过 `/api/tags` 获取已安装模型，按模型声明上下文长度、最大输出 Token 和文本/视觉能力，并允许为不同 AI 任务选择 Ollama 模型。

## P2 Docker 部署与独立鉴权

- [x] 保持单仓库和同一套业务代码，通过显式 `AUTH_MODE=fnos|local` 选择 fnOS 网关身份或 Docker 本地身份；配置缺失或非法时拒绝启动，禁止自动降级为无鉴权管理员。
- [x] 将请求身份统一为与部署方式无关的用户和角色模型，业务权限统一读取 `isAdmin` / `isAdministrator()`；数据库和 API 暂保留 `isGatewayAdmin` 兼容旧版，同时保持 fnOS 现有权限行为不变。
- [x] Docker `local` 模式重新启用本地账号登录、退出、Cookie 会话解析和登录失败限流，fnOS 模式继续关闭独立登录。
- [x] Docker/本地账号使用 `admin/admin` 首次登录，首次登录强制改密；支持管理员创建、重置本地账号，并撤销目标账号会话。
- [x] 补齐本地管理员修改密码、会话撤销、过期会话清理和忘记密码后的 Docker 主机离线重置流程。
- [x] Cookie 会话增加 CSRF 防护、可信反向代理配置和 HTTPS/Secure Cookie 验证，禁止 Docker 模式信任客户端伪造的 `X-Trim-*` 网关身份头。
- [x] fnOS 与 Docker 备份格式保持兼容；跨部署恢复后允许当前可信管理员重新绑定数据所有权和成员权限，并留下审计记录。
- [x] 增加多阶段 `Dockerfile`、`.dockerignore`、非 root 运行用户、健康检查、优雅停止和持久化目录约定。
- [x] 提供 `docker-compose.yml` 示例，明确数据库、报告原件、备份、日志和配置卷，以及端口、时区、AI/OCR 配置和反向代理接入方式。
- [ ] 明确 OCR 运行时的镜像策略和体积上限，验证 `linux/amd64` 与 `linux/arm64` 的 RapidOCR/OpenVINO 或兼容后端可用性。
- [x] 增加 Docker 空库初始化、重启、升级、备份恢复、权限隔离、伪造身份头、CSRF、登录限流和会话撤销测试。
- [x] 在 GitHub Actions 中构建并发布 OCI 镜像到 GHCR，至少提供 `vX.Y.Z`、`X.Y`、`latest` 和提交 SHA 标签；tag 版本必须与 `package.json` 一致。
- [x] 发布 `linux/amd64`、`linux/arm64` 多架构 manifest，并为镜像添加源码、版本、许可证等 OCI 标签以及 SBOM、来源证明和签名。
- [x] GitHub Release 同时发布 fnOS `.fpk` 和对应 Docker 镜像说明，记录应用版本、数据库 schema、支持架构、升级命令和不兼容变更。
- [x] 编写 Docker 安装、首次登录、HTTPS 反向代理、升级、回滚、数据目录权限、备份和故障排查文档。

## P3 发布验收

- [x] 单元测试：日期排序、游标分页、字段校验、权限、端口和 AI 输出。
- [x] 集成测试：图片/PDF 上传、OCR/AI、重试、通知、回收站和备份恢复。
- [ ] 安全测试：伪造网关头、路径穿越、越权访问、CSRF、登录限流和会话撤销。
- [ ] PC 1440×900、手机 390×844、平板横竖屏视觉与触控验收。
- [ ] fnOS 真机安装、升级、停止恢复、端口修改、端口冲突和卸载数据策略。
- [ ] OCR 连续任务耗时、内存、低配置 NAS 和不同 CPU 架构验证。
- [x] 替换正式应用图标。
- [ ] 完善隐私说明、使用说明和发布记录。

## 已确定的产品约束

- 时间轴只按医院报告生成日期排序，上传时间仅用于审计。
- 每次上传默认是一份可包含多页的报告，混合报告需要拆分或人工确认。
- AI 可以生成中性事实摘要，但不能给出疾病诊断、风险评分或治疗建议。
- 视觉 AI 默认关闭，本地 OCR 文本是检索和证据追溯的基础。
- 普通用户可以创建和管理自己的家庭成员；成员授权和全局运行配置由 fnOS 系统管理员管理。
- 普通删除进入回收站，30 天后才永久清理。
- 首版提醒仅在应用内展示。
