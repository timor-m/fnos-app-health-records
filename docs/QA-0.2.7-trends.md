# 0.2.7 趋势迭代验收（2026-09-10）

## 上传分组：后端接入

- 三种多文件分组模式已接入浏览器和 NAS 导入；单文件浏览器流程保持原状。取消单份报告 24 文件限制，保留 1000 文件、2GB/组及 40MB/文件资源保护。
- 合成 30 张图片加 PDF 测试覆盖完整提交、页序、不完整组禁止创建报告、重试去重、重启、权限撤销和暂存清理；上传回执纳入未发布 v18 结构，已有 v18 测试库备份后幂等补表，不新增编号。
- 已通过全量测试、类型检查、前后端构建及发布校验；尚未进行本轮浏览器、fnOS 网关、Docker 反代和真实 OCR/AI 大批量验收，不代表下方历史验收已覆盖此功能。
- 消融实验：复用既有入库、权限与识别队列，不新增第二套识别流程；目前不做刷新后自动续传或文件内分片。

## 上传分组：文件夹交互预览（历史阶段，后端状态以上节为准）

- 合并/独立列表鼠标超过移动阈值即可拖拽，避免长按前移动直接取消；触屏保留长按，普通滑动不排序。首行高亮继承容器圆角。根目录文件互拖在松手时建组（目标文件在前、拖入文件在后），悬停和取消不建组。新增执行真实组件函数的鼠标拖动/跟手坐标/排序/撤销及互拖建组测试；尚未完成浏览器与触屏真机验收。
- 排序补充跟手预览，原卡片淡化，使用稳定网格捕获指针；空白区域长按触发，表单、标题、缩略图不触发。图片禁用原生拖拽/选中。长按移动、取消及离开页面清理交互状态。10 项专项测试、类型检查及前端构建通过；触屏长按/滚动兼容及实际拖拽视觉仍待真机验证。
- 自定义分组使用单层报告文件夹；根目录同时显示文件夹与未分组文件，支持返回上级、多选建组、移动、组内排序及剩余文件各自成组。移除文件夹仅解散归属，不删除文件。
- 图片点击复用 ImageViewer，复选框独立选择。无预览地址的 PDF/NAS 文件明确显示暂不支持预览，不调用新接口。
- 新模式仍禁用批量提交，原单文件/合并提交保留；分组仅保存在当前页面内存，刷新后需重建。现有上传数量及大小限制未调整。
- 6 项专项测试通过，包含执行组件分组状态逻辑的移动、解散及文件移除测试；类型检查、前端构建通过。未做浏览器视觉或真实设备验收，本地服务保持停止。
- 进行消融实验：移除上下分离的已分组列表，保留一个浏览区域，复用查看器，不增加嵌套目录或服务端接口。

## 通用日期选择器滚轮回归

- 补充鼠标/笔按住上下拖拽：五列共用事件，松开、取消或丢失捕获后吸附，拖拽期间不触发定时吸附；触屏继续原生滚动。关闭时释放指针捕获。新增事件接入与清理回归测试；未进行实际鼠标设备验收。
- 保留外观，移除 CSS 强制吸附与平滑滚动的双重控制；鼠标单次事件最多切换一项，触控板小增量累积，触屏惯性结束后再定位。
- 月份天数变化同步重定位日列，确认前校验月底/闰年日期；关闭和卸载清理定时器。
- 新增 5 项测试（步进、反转、循环、日历边界、单一吸附与清理），全量 572/572、类型检查、前端构建、差异检查通过。
- 按用户要求保持本地服务停止；未验证真实鼠标、触控板及触屏手感，未重新进行 fnOS/Docker 安装。
- 进行消融实验：删除重复吸附及滚动中重定位逻辑，不新增日期 UI 或依赖。版本仍为 0.2.7，无数据库变更。

## 指标状态、反馈和收起提示

- 治理待核对原因取目标记录的共享准入结果，混合分组切换代表记录时同步原因，不再被字典匹配文案覆盖。新增与报告详情原因一致的集成断言。
- 报告指标说明、治理原因和趋势提示共用“！”：默认收起，点击展开，失焦、外部点击、Escape、外部滚动关闭；不新增遮罩。保存数量及请求错误保持可见，批量处理的详细原因收起。
- 全量测试 565/565 通过；类型检查与应用构建通过。浏览器独立标签使用既有合成报告，验证展开、Tab 失焦和 Escape 关闭，没有修改记录。
- 本轮窄屏模拟未实际改变视口，不能计为触屏验收；尚需触屏设备验证点击外部关闭、短屏边界。未重新安装 fnOS/Docker，未验证真实报告 OCR。
- 进行消融实验：移除趋势页旧提示弹层的状态、事件和样式，只保留共享小组件；不扩展趋势图、准入规则或数据库结构。版本保持 0.2.7。

## PC 统一全屏三列

- PC 核对入口复用全屏三列：左侧原件及可用 OCR 证据原文、中间指标列表、右侧编辑表单；自动选中目标指标，移除直达模式独有的两列尺寸覆盖。触屏继续隐藏列表，仅展示单层编辑。
- 浏览器独立标签页实测 2560×1181 下工作区满屏、三列可见且仅一个 dialog，避免刷新用户已有编辑标签。合成记录无原件，未验证真实 OCR 定位。
- 9 项相关测试、类型检查和构建通过；版本仍为0.2.7，未进行 NAS 安装验收。进行消融实验，删除重复布局规则，沿用原工作区。

## 最新：直达单层指标核对

- 治理入口传入目标报告与指标 ID，复用 ReportDetail 的原件和编辑区域，直达模式不渲染报告详情或全部指标列表。档案页原入口不受影响。
- 本地浏览器合成数据实测：dialog 仅“核对指标”1 个、打开编辑区1个、报告详情外壳0个、指标列表0个，目标名称正确带入；保存后 dialog 为0，待核对条数由5降至4。
- 9 项相关 UI 测试、类型检查、生产构建和差异检查通过。未重跑全量测试、未安装 NAS，合成数据无原件页，不作为真实 OCR 高亮验收。版本保持0.2.7。
- 进行消融实验，复用已有表单和保存接口，仅增加受控直达模式，不新建第二套编辑器；本节替代此前“只打开报告详情”的交互决定。

## 治理入口弹窗复用修正

- 移除报告加载后自动打开指标列表和编辑区的逻辑；档案页与治理页共同使用 ReportDetailSheet，保留原详情操作流程。
- 本地浏览器点击“核对指标”后实测：报告详情 1 个，全部指标弹窗 0 个，打开的指标编辑区 0 个。
- 类型检查、生产构建、差异检查通过。全量测试 563 项中 562 通过，1 项 OCR worker 超时断言失败；该测试与相关 UI 测试独立复跑共 10 项全部通过。不据此宣称全量首次通过；未安装 NAS。
- 进行消融实验，删除两次自动打开和重复弹窗外壳；仅提取双方实际共用的详情弹窗，不改变业务准入规则。版本保持 0.2.7。

## 全部结构化指标触屏弹窗

- 触屏取消固定 92dvh，改为内容高度、最大 88dvh；列表超出后滚动，PC 全屏三列不变。标题区按钮不参与压缩，修正“补充”被通用标题栏按钮宽度限制的问题。
- 本地合成单条记录实测高度约 235px，底部对齐视口，无大块空白。8 项相关 UI 测试、类型检查、生产构建和差异检查通过；未做 NAS 真机或大量指标滚动验收。
- 版本仍为 0.2.7，已更新 changelog。进行消融实验，去掉不必要的固定高度，不增加业务逻辑。

## 核对弹窗高度与操作区

- 触屏编辑区使用统一主题背景、遮罩、顶部圆角和拖动条；高度自适应、最大 88dvh。表单字段独立滚动，底部操作区不参与滚动；取消、保存和查询去掉独立圆角覆盖。
- 浏览器实测：375×667 下弹窗约 555px（小于最大约 587px），底部按钮圆角均 999px；320×500 下弹窗达到 440px 上限，字段区域 244px/内容 359px，底部按钮仍可见。只做合成数据布局检查，未修改报告。
- 类型检查、生产构建、8 项相关 UI 测试和差异检查通过；未重跑全量测试或安装 NAS。保留 0.2.7，无迁移。
- 进行消融实验，去掉不必要的抽象和设计：只分离滚动内容和固定操作区，复用原主题变量与全局按钮样式。

## 指标编辑表单视觉收敛

- 名称和结果原文优先显示，数值与单位双列；标准匹配、参考范围/代码等使用原生折叠区，保留全部字段与原保存逻辑。统一输入、说明文字及按钮尺寸，不增加新表单组件。
- 本地深色主题 375px、320px 已查看折叠态，核心信息与保存按钮可见，无横向溢出。展开态自动化未验收成功（CDP 点击超时），不宣称完整交互或 NAS 真机通过。
- 8 项相关 UI 测试、类型检查、生产构建和差异检查通过；未重跑服务端全量测试。版本保持 0.2.7，更新 changelog，无 migration。
- 进行消融实验，去掉不必要的抽象和设计：复用原生 details 和现有主题样式，仅收敛当前核对表单。

## 治理列表触屏样式修正

- 视图切换独立为分段按钮；复制/反馈使用次要按钮；单条记录的信息和操作分行，窄屏操作区双列排列，避免纵向断字及横向溢出。
- 浏览器检查深色主题 375px、320px 和 1280px 布局：窄屏页面无横向溢出，操作按钮文字横排；桌面保留右侧操作布局。只使用合成数据，不触发复制或外部反馈。
- 相关 UI 回归 7 项通过，类型检查和生产构建通过；本次纯样式未重跑服务端全量测试、未安装 NAS。更新 changelog，版本保持 0.2.7。
- 进行消融实验，去掉不必要的抽象和设计：仅调整本页结构、局部样式与冗长文案，不新增组件库或业务流程。

## 最新交互验收：无需标准映射的核对入口

- 治理列表“核对指标”打开代表报告并定位待核对记录，沿用原件对照/编辑组件；标准字段明确可选，主按钮为“保存并重新判断”。混合组优先打开未通过的记录，显示待核对条数；全部可用的组放入可选标准化视图。
- 本地浏览器使用隔离合成记录，实际执行：打开核对入口 → 保留“不指定标准指标” → 保存 → 报告可用于趋势计数由 0 变为 1 → 趋势页出现机构内未标准化卡片。使用键盘激活按钮，鼠标自动化遇到 CDP 超时；未据此宣称完整触屏/鼠标验收通过。
- 实际保存发现历史 evidence 为对象时 HTTP 500，已修正数组兼容并将该格式纳入回归。保存只确认单条记录，不把整组标为已确认。
- 最新全量 560 项测试、类型检查、生产构建和差异检查通过；发布校验通过。未重新打 FPK、构建 Docker 或安装 NAS。本次合成记录无原件页，不作为真实原件定位或整轮响应式验收。
- 消融实验：复用现有报告核对组件和保存接口，无第二套编辑器、无新增业务接口和数据库迁移；版本保持 0.2.7。

## 校对与治理闭环补充

- 校对保存后显示准入结果或具体阻断原因；映射反馈和历史预览使用共享准入规则，不能以匹配等级代替结果核验。
- 人工排除优先于单条标准映射，编辑被排除记录返回明确提示，先撤销排除后才能修改。不新增接口或 migration。
- 扩展合成数据回归：机构内数值计入预览、排除后编辑无副作用、撤销后修正恢复、数值与结果文本冲突阻断、治理反馈与实际趋势一致。
- 全量 560 项测试、类型检查、生产构建、发布校验及差异检查通过。本次未执行浏览器交互、NAS/Docker 部署或重新打包安装验收。
- 进行消融实验，去掉不必要的抽象和设计：复用现有保存返回的报告详情和通知，不增加状态接口或新的管理界面。

## 最新补充：统一准入与未标准化数值通道

- 报告详情与趋势列表调用同一准入函数；可靠未标准化结果进入原有卡片，标准身份歧义、单位冲突、排除决定不能绕过。
- 仅按相同成员、机构、原始名称、章节、单位、方法及标本聚合。不写入标准字典或机构关联表；方法或标本不明时禁止默认连线及涨跌判断。
- 新增合成数据测试覆盖证据缺失/不一致、限值表达、不同口径隔离、详情与趋势一致、人工编辑恢复、排除退出、置顶及越权访问。
- 本次验证：560 项自动测试、`npm run typecheck`、`npm run build`、`npm run validate:release`、`git diff --check` 通过。版本仍为 0.2.7，未新增 migration。
- 本次未重新安装 NAS、构建 Docker 镜像或执行浏览器真机视觉验收；既有测试包和运行实例不能视为包含本次变更。下文各轮历史验收结论不替代本次部署验证。
- 消融检查：保持原有卡片与单图结构，仅追加轻量标识和必要口径信息；不恢复机构管理、关联规则及多折线接口。

## 最新范围修正

用户反馈新增 UI 复杂且偏离原需求，现已撤回机构内管理区、多口径替换图表、待确认列表及对应服务端接口/自动关联。趋势页恢复原有布局。下文相关功能和截图验证仅为撤回前的历史记录，不再作为当前功能清单。保留指标准入、单位证据、参考范围和治理反馈修复；schema v18 保留兼容，原数据不删除。撤除源码已另存本地临时备份，可恢复。

撤回后验证：558 项测试、类型检查、生产构建和发布校验通过。浏览器确认原有筛选、最新值、图表、变化范围与近期记录恢复，新增管理区域和按钮消失。本地服务已重启；撤回前的 NAS 安装及 Docker 镜像未自动更新，不作为本次撤回验收结果。

状态：**实施及验收中，未发布，不代表整轮验收通过。** 应用统一为 0.2.7 - Unreleased，最新目标 schema v18；不回退已迁移数据。下方旧轮次 v17/559 项等记录保留当时事实。误标测试包的历史记录见 [此前 fnOS 验证](QA-0.2.8-fnos.md)。

## 本地检查

- 全量自动测试：559 项通过。覆盖参考范围准入分离、人工治理、历史重评估、机构边界及成员权限、关联追加/解除/改归、整批失败回滚、同报告不同方法不折叠、图表日期/单位隔离及 v16 → v17 备份迁移。
- 类型检查、前端构建、发布校验、差异空白检查通过。前端主包仍有超过 500 kB 的构建提示，不影响构建。
- fnOS FPK 构建及包结构校验通过，生成 `dist/fnos-app-health-records-0.2.7.fpk`。
- Docker Hub 基础镜像查询长时间无进展；以 `NODE_IMAGE=public.ecr.aws/docker/library/node:22-bookworm-slim` 构建成功，未修改默认来源，未推送镜像。

## 隔离 Docker 验证

使用独立测试容器和合成记录，不复制用户报告。

- 健康检查正常，本地登录与强制改密接口通过，内置浏览器可登录并打开趋势。
- 三个机构内口径可人工选入同图；最多两种单位同时显示，第三种受限；取消一种后可选择另一种，左右轴提示可见。
- 图例可用键盘切换，数据点可打开来源报告。
- 追加表单仅列同机构同单位候选，项目口径锁定；确认后关联数量从 2 增至 3，数据库验证一致。
- 工具实际视口为 480px 时显示触屏导航，DOM 页面宽度无横向溢出。工具视口与请求尺寸不一致，**不计作 390px 真机视觉验收通过**。

## fnOS 安装后验证

- 用户已手动安装 0.2.7；应用中心与服务端 manifest 均确认版本为 0.2.7，sub_version 为 0.2.7.0。
- 安装后数据库仍为 v17，完整性检查为 ok，原有报告数量仍为 1，机构内项目为 0；未修改原报告。
- 带网关前缀的健康检查返回 200；匿名访问机构内趋势接口返回 401。
- 本次覆盖前已额外备份现有 v17 数据库到测试机 `health-records-upgrade-backups/pre-027-correction-v17-20260910.sqlite`，权限 600。
- 前次扩展上传权限阻挡已由用户手动安装解决，无需再次上传。自动化点击应用中心“打开”尚未成功进入新版本应用窗口，网关业务操作验收仍待继续；未伪造身份或绕过鉴权。

## 本地直接启动验收

按用户要求切换为本地服务，不再将飞牛窗口自动化作为前置条件。

- 本地地址：`http://127.0.0.1:14335/app/fnos-app-health-records/`；独立临时数据目录，使用 12 条合成指标，不读取 NAS 报告。
- 标准指标 6 个点按两种方法在一张卡片显示两条同单位曲线；图例关闭一种方法后只显示对应的 3 个点，来源入口可打开合成报告。
- 单条关联改归、取消解除、确认解除完成 UI 操作及 SQLite 复核；改归后目标 4 条，解除后 3 条，刷新保持，原始指标始终 12 条。
- 原生确认框阻塞内置浏览器自动化，已复用全局应用内确认组件替换。此改动仅在当前本地代码及构建中，未重新安装到 NAS。
- Chrome 实际视口 390×844 与 1440×900 检查通过，页面无横向溢出；390px 完整页面截图检查及图例显隐通过。此为响应式模拟，不代表 iOS/Android 真机验证。
- 合成数据不包含原件和真实 OCR，报告回看不等于原件区域高亮验收；无 OCR 证据的合成点在详情页待核对，不能用此样本证明完整提取准入一致性。
- 修改后再次通过 559 项自动测试、类型检查、本地生产构建、发布校验和差异检查。版本仍为 0.2.7 - Unreleased。

## 本轮补齐与验证

- 成员范围“待确认 N 项”入口、机构内转标准只读预览、主动启用的精确自动关联规则已实现。规则确认人权限撤销、多目标冲突、未知标本/方法或证据不足不自动关联；手动解除保持不加回。
- 新增 v18 规则及人工决定表；v16/v17 升级备份与默认关闭规则回归通过，既有 v17 迁移不回改。
- 单位证据读取本地规划器记录的单位列/表头关系，并回查同报告 OCR 行。补充来源页码处理真实 OCR 每页重复行号；旧证据无法唯一定位时继续待确认。不按数值大小推断，不将 mg/L 当成 g/L。
- 本地 Chrome 验证待确认 3 项、打开对应来源报告、3 条机构记录的标准化预览、启用及关闭自动规则；390×844 和1440×900 无页面横向溢出。使用隔离合成数据，不改变 NAS 用户报告。
- 真实本地 RapidOCR ONNXRuntime 对合成 PNG 识别成功，约1.6秒，返回两项结果及 mmol/L 单位、坐标。此为真实 OCR 冒烟，不等于完成真实 OCR＋AI＋趋势全链路；本机 localhost:1234 LLM 服务连接被拒绝。
- 全量563项测试及类型、发布、FPK包结构校验通过；最终产物仍为0.2.7，未推送镜像或发布 Release。
- 最新 Docker 镜像本地重建成功，独立空数据容器启动、健康检查、schema v18 及完整性检查通过，匿名访问待确认接口返回401。容器验证后停止，保留隔离测试数据；本地直接启动页面继续可用。

## 待完成

- fnOS 网关内完整业务操作和普通查看者验收（纠正版本覆盖安装及服务检查已通过）。
- 平板横竖屏、真实触屏交互及原件定位验证；本地 PC/390px 模拟、单条解除、改归及刷新保持已验证。
- 带原件/OCR 证据的完整提取链路验收；现有合成记录验证覆盖存储、趋势展示和报告回看，不包含 OCR/AI。
- 最新 v18 包在 NAS 的覆盖升级和网关完整业务验收；当前 NAS 的 v17 验证记录不能代替 v18 验收。Docker 已构建并验证空库启动，升级场景由服务端迁移测试覆盖，尚未完成容器内整轮业务验收。

## 消融检查

- 关闭规则后不自动关联；唯一精确匹配及可核验证据才能归入；冲突目标、缺证据、方法/单位不符均留待确认，人工解除后重复执行不加回。
- 移除表头/单位列来源或让来源指向错误页，单位核验仍拦截；恢复已验证的来源后准入，单位前缀差异不误认。未引入自动聚类、评分或数值猜单位。
- 预览复用已有归一化逻辑，测试确认原始指标、人工关联与归一化记录均不被预览改写；未新建第二套编辑表单。

沿用现有单序列图、权限服务、原件入口和选择器，不增加通用图表引擎或自动聚类。关联失败样本验证整批不落库；同单位共轴、不同单位独立刻度与方法隔离保留，不能为减少图例而删除这些边界。
