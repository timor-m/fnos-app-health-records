# 档案存储位置：阶段验收

日期：2026-09-11。应用版本保持 0.2.7，schema 不变。目标是另一存储空间，不限定 RAID。

## 本地结果

- 617 项测试通过，类型检查、差异检查及 FPK 打包校验通过。
- 修复配置路径改变后仍复用旧数据库连接的问题：未显式关闭旧连接前拒绝访问；关闭后才可打开目标数据库。
- 新增连接切换回归；修正测试自身切换临时目录前未关闭连接的问题。
- 未增加自动迁移入口，不允许手动改目录配置替代完整迁移流程。

## 飞牛测试机结果

- 存在两个独立存储空间，应用及档案仍在原空间。
- 已停止应用生成完整数据备份，压缩包完整性校验通过；源数据未删除或迁移。
- 在另一空间创建独立空测试目录，应用账号写权限检查为拒绝，应用授权目录快照为空。未通过 root 改权限或伪造授权快照。
- 用户完成应用中心授权后复查：授权快照已包含测试目录，应用账号读取、写入权限检查均通过。
- SSH 重连后，以应用账号（UID 994）执行独立探针：文件写入、fsync、重命名、读取、SQLite WAL、双连接写锁互斥、锁释放后提交、关闭重开及 integrity_check 全部通过。探针临时目录已清理，未修改现有档案。此结果验证当前本地文件系统与应用授权，不代表断电可靠性或完整迁移验收。
- 同版本 `install-fpk` 没有更新现有服务端产物，远端与本地新产物哈希不同，不能标记安装升级验收通过。
- 启动命令曾返回 10330；日志显示原启动脚本等待 1 秒后过早失败，随后进程已监听。再次启动确认应用中心状态 running，HTTP 健康检查成功。

## 第二阶段本地验证

- 后台等待请求与任务排空、SQLite checkpoint、复制与双向哈希、原件引用检查、持久化切换和原库保留已实现。
- 627 项全量测试通过；存储专项覆盖取消、缺失原件拒绝切换及修复后重试、切换后恢复不覆盖新数据、路径别名、鉴权、Docker 拒绝和备份恢复保留本机状态。
- 构建后的真实 HTTP 服务完成隔离迁移：提交、轮询、切换、原库保留及恢复访问通过。
- 类型检查、差异检查、前后端构建及 FPK 校验通过。没有使用真实报告作为测试样例。
- Docker 完全不展示存储模块，路由拒绝访问，存储设置 API 不提供；仍由宿主机挂载配置管理。
- 消融复核：沿用现有任务运行状态和全局确认组件，未增加通用迁移框架、数据库版本或自动清理功能；移除 Docker 存储页面分支。

## 第二阶段真机安装记录

- `install-local` 不能当作同版本原位升级：它先停止并卸载已有应用，再因缺少安装环境参数中止。今后禁止用此命令试探覆盖安装。
- 已使用 FPK 加安装环境参数恢复应用；服务状态 running、HTTP 健康检查正常，服务端产物哈希与该测试包一致。原数据库及此前独立备份仍在，未执行数据恢复覆盖。
- 重新安装后应用授权目录快照为空，需要用户重新授权目标目录。未自行修改授权快照或用 root 绕过读写权限。
- 实际档案迁移尚未执行；安装过程不作为无损原位升级验收通过的依据。

## 未完成 / 放行条件

- 已通过：飞牛应用中心目录授权及应用账号实际文件、SQLite 读写验证（不是以 root 执行探针）。
- 无卸载的旧版本升级路径仍需另行验收；启动就绪检测已改为有界等待。
- 完整迁移流程已完成本地验证及真机正常跨空间迁移；重装接管尚未实现。
- 断电、磁盘空间不足、授权撤销与恢复、Docker 部署端到端回归尚未验收。

## 第三阶段：真机跨空间迁移

- 用户重新授权后，应用级授权快照及应用账号目标写权限均通过；已有独立备份再次通过压缩包完整性检查。
- 通过已登录的飞牛网关页面选择目标目录、查看预览并确认迁移，没有绕过管理员鉴权或手动修改目录指针。
- 28 个文件（约 9.7 MiB）复制及校验完成，迁移状态 completed，无错误；刷新维护页后正常返回存储设置。
- 档案已从原安装空间迁入第二存储空间的专属目录；运行目录与 OCR 环境保留原位，源数据未删除。
- 迁移前后报告、页面、指标、成员数量一致，目标数据库 integrity_check 通过。
- 通过飞牛网关读取全部 23 个原件成功，响应正常且内容非空；未输出报告名称、内容或检查结果。
- 应用中心正常停止、重新启动后，网关返回 ready / relocated / completed，维护暂停已解除；再次检查数量一致、数据库完整性与 HTTP 健康检查均通过，源库仍保留。

结论：真机正常迁移、数据数量一致性、数据库完整性、原件读取及正常重启复查已通过；断电等故障专项和重装接管尚未完成，不等同于整轮验收完成。本轮只更新验收记录与 TODO，未修改业务代码、版本或 schema。

## 重装接管：隔离检查结果

- 使用独立临时目录和合成 SQLite 数据模拟“档案保留、运行目录重新创建”，未卸载真机应用、修改现有指针或访问真实报告。
- 保留原运行目录中的位置配置时，解析器可找到已有档案；新运行目录缺少该配置时，只选择新的默认目录，不会发现另一存储空间中的已有档案。
- 当前存储接口只有预览、迁移、继续和取消，没有已有档案接管操作。因此本项不能验收通过，也不能以正常重启成功替代重装接管测试。
- 下一步需实现管理员选择授权目录中的已有档案、身份与数据库/文件/密钥完整性检查，以及安全绑定；还需明确重装后账号身份变化的权限处理。实现之前不做真实卸载试验。
- 临时合成数据已清理；业务代码、版本和 schema 未变。

## 旧副本手动清理：本地验收

- 新迁移在切换前记录已校验文件清单与源目录身份；仅成功切换后提供管理员清理预览和二次确认。
- 逐文件核对后删除旧数据库、原件、缩略图、报告导出及上传暂存；运行配置、密钥、OCR、备份、新增文件及内容变化的文件保留。不会递归删除旧目录。
- 覆盖重复请求、旧数据库使用痕迹、任务不匹配、清单缺失、符号链接替换、保留新档案数据及缺少 API 删除确认等回归。
- 639 项全量测试、类型检查、前后端构建、差异检查通过；构建后的隔离 HTTP 服务完成迁移、清理预览、确认删除旧文件及健康恢复检查。
- 消融复核沿用迁移状态和全局确认组件，未引入通用清理框架或额外后台任务系统。
- 未在真机部署或删除旧文件；此前真机迁移没有清理清单，升级后也不能直接开放一键删除。该兼容边界已在页面与使用文档说明。
