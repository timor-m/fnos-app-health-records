# Agent 工作规范

本文档是健康档案仓库的 Agent 工作入口。后续进行功能开发、问题修复、版本发布或指标字典维护时，先遵循本文件，再按专项文档执行。除非用户明确要求，否则不要回滚工作区中已有的其他改动。

## 基本原则

- 先阅读相关代码、测试和文档，再修改文件；优先沿用仓库已有的服务、组件、迁移和脚本。
- 变更保持最小范围，避免顺手重构无关模块。
- 修改前确认当前工作区状态，保留用户已有改动；修改后检查 `git diff`、`git diff --check` 和测试结果。
- 用户只要求分析时不直接写代码；用户要求落地时完成实现、测试和文档更新，不停留在方案阶段。
- 涉及健康数据、原件、OCR、AI 输入或日志时，不能把成员、医院、报告、检查结果、原文或凭据写入 Issue、Release Notes、日志和示例。
- fnOS 应用和 Docker 是两种运行环境：不要把 fnOS 网关账号、应用授权目录或 fnOS 专属提示泄露到 Docker 流程，也不要让 Docker 本地账号逻辑破坏 fnOS 网关鉴权。

## 常规开发流程

1. 明确需求属于功能、新修复、发布、数据库、指标字典还是部署适配。
2. 检查当前分支、工作区改动、应用版本、schema 版本和相关文档。
3. 搜索现有实现和测试，确定影响范围；跨模块改动时先列出接口、数据和 UI 影响。
4. 修改代码，并为共享逻辑、数据格式、迁移和用户可见流程补充对应测试。
5. 更新必要文档和 `CHANGELOG.md` 的当前 `Unreleased` 区块。
6. 执行与风险匹配的验证：至少运行相关测试、类型检查或构建，并检查差异和敏感信息。
7. 汇报改动、验证命令、未运行的检查以及剩余风险。

数据库、Docker、fnOS 打包和发布的具体要求分别见：

- [应用发布与数据库升级流程](docs/RELEASE_PROCESS.md)
- [版本与数据库迁移规范](docs/VERSION_MIGRATION.md)
- [Docker 部署](docs/DOCKER_DEPLOYMENT.md)
- [指标字典说明](dictionary/README.md)

## 版本与发布流程

### 新功能和修复

- 所有用户可感知的新功能、重要修复、部署行为变化和兼容性变化，都要在当前 `CHANGELOG.md` 的 `x.y.z - Unreleased` 区块中简要记录。
- 条目按 `Added`、`Changed`、`Fixed` 分类，描述用户能感知的结果；数据库变化必须写明 schema 版本变化和升级影响。
- 条目记录规则：
  - 指标字典调整（新增指标、别名、分类、快照 revision 等）不纳入 changelog，字典变更以字典自身的 revision 和 manifest 为准。
  - UI 调整细节不逐条展开，合并为一条“优化 UI：……”概述即可。
  - 条目只罗列核心功能点和用户可感知的结果，不描述问题成因、排查和修复过程。
- 不要为了普通功能修改而提前创建正式版本章节；未发布内容统一进入当前版本的 `Unreleased` 区块。
- 检查 `template.config.json` 的 `releaseNotes` 是否仍准确，特别是摘要、亮点、目标数据库版本和部署方式。

### Release 之后继续迭代

- 一旦版本已经创建 Release、tag、fnOS 安装包或对外测试包，该版本即视为已发布并冻结。
- Release 之后的任何新功能或修复必须进入更高的新版本 `Unreleased` 区块，不能继续追加到已经发布的 `## x.y.z` 内容中，也不能修改已发布版本的变更说明来掩盖后续改动。
- 发布新版本前，使用 `npm run release:notes` 确认生成内容只来自当前 `package.json` 版本对应的 changelog 区块。
- 使用 `npm run version:bump -- patch|minor|major` 或仓库约定的发布命令升级版本；版本号必须同步 `package.json`、`package-lock.json`、fnOS manifest、Docker 镜像标签和 Release tag。
- 发布时冻结当前版本区块为 `## x.y.z`，自动创建干净的下一个 `## x.y.z - Unreleased` 区块。禁止把上一版本条目整体重命名到下一版本。
- 版本发布前至少执行 `npm run validate:release`、`npm test`、`npm run typecheck`；完整产物按 `docs/RELEASE_PROCESS.md` 验证。
- 已发布数据库 migration 不得修改；结构或数据语义变化必须新增 migration，并同步更新 schema、测试、changelog 和迁移文档。

发布前最小检查：

```bash
npm run release:notes
npm run validate:release
npm test
npm run typecheck
git diff --check
```

## 未录入指标处理流程

当用户直接贴出“未命中指标名称”“指标字典收录申请”或类似列表时，不能直接把全部名称写入远程字典。必须先分析和对比，再决定是否收录。

1. 清理输入：去重、保留原始名称、识别前缀序号、括号代码、大小写、空格、全角半角和常见 OCR 噪声；保留原始名称用于反馈，但不要把结果、报告或成员信息带入分析。
2. 对照本地核心字典和当前远程字典，检查标准名称、可信别名、英文代码、分类、指标类型和单位；优先确认是否只是已有指标的机构写法或别名缺失。
3. 判断是否为可收录的定量/定性指标。符合以下条件才进入远程字典候选：
   - 名称具有明确医学指标语义，而不是器官、部位、查体描述、科室小结、结果判断、表头或普通叙述。
   - 与现有标准指标不是重复项、不同值类型误合并项或仅有单位差异的同一项目。
   - 能确定标准名称、指标类型、分类、别名和必要的单位兼容规则；不确定时进入问题池，不猜测收录。
4. 排除形态和叙事内容：皮肤、胸廓、心音、腹部、肝脏、肺脏、神经系统、下肢静脉等检查部位或描述通常属于报告专属内容/形态发现，不应加入趋势指标远程字典。
5. 特别检查边界：百分比与绝对值、收缩压与舒张压、空腹/餐后/随机、不同标本、不同方法、左右侧和不同计量含义不得因为名称相似而合并；单位不兼容时保守拆分。
6. 对候选项做跨机构对比，确认名称、英文代码、单位和报告上下文在不同机构中表达的是同一医学项目；仅出现一次且语义不明确的名称，不直接发布。
7. 将确认可收录项加入远程字典的完整快照，更新 revision、manifest、分类引用、别名和校验；必要时补充字典测试。
8. 对不能收录的项说明原因，保留在未命中问题池或作为独立形态/报告章节处理；不要为了减少问题池数量而伪造标准指标。
9. 发布远程字典前运行字典校验、比较和构建命令；确认远程快照可独立下载、校验、安装、回滚，并能在本地回填历史指标。

常用检查命令：

```bash
npm run dictionary:validate
npm run dictionary:compare
npm run dictionary:manifest
npm run dictionary:build
```

指标收录申请的输出应至少包含：原始名称、是否命中已有指标、建议标准名称、建议别名、分类/类型、单位兼容性、是否建议加入远程字典，以及排除或待确认原因。外发 Issue 只允许包含未命中的指标名称和必要的版本/数据库版本信息，不包含任何健康数据。

## 交付记录

每次完成工作后，在最终说明中简要记录：

- 修改了什么，以及涉及的文件。
- 是否更新 changelog、版本或迁移。
- 执行了哪些测试和构建检查。
- 哪些检查因环境、网络或外部服务未执行。
- 是否存在需要用户在 fnOS、Docker、远程字典服务或 GitHub Actions 中继续验证的事项。
